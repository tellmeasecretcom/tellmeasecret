import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  productionBrowserSourceMaps: false, // <-- Source Maps für Livegang deaktiviere
};

export default nextConfig;
